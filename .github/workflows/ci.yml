name: ci

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build-binaries:
    runs-on: ubuntu-20.04

    steps:
    
    - name: Checkout
      uses: actions/checkout@v2

    - name: Build & Make Debian Package
      run: make package-debian
